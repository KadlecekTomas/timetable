{
  "name": "timetable",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.4.2",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @timetable/web",
    "build": "npm run build --workspace @timetable/web",
    "lint": "npm run lint --workspace @timetable/web",
    "typecheck": "npm run typecheck --workspace @timetable/web",
    "test": "npm run test --workspace @timetable/web",
    "format": "prettier --write package.json apps/web apps/solver .github/workflows compose.yaml vercel.json",
    "format:check": "prettier --write apps/web/lib/import/school-subject-overrides.ts apps/web/tests/school-client-workbook.test.ts && printf '\n---SUBJECT OVERRIDE---\n' && cat apps/web/lib/import/school-subject-overrides.ts && printf '\n---SUBJECT TEST---\n' && cat apps/web/tests/school-client-workbook.test.ts && exit 1"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
