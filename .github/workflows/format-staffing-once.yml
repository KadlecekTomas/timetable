name: Format staffing once

on:
  push:
    branches:
      - agent/manual-teacher-card-save

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/manual-teacher-card-save
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Format changed files
        run: npx prettier --write apps/web/app/staffing/page.tsx apps/web/e2e/staffing.spec.ts
      - name: Commit formatted files and remove one-time workflow
        run: |
          rm .github/workflows/format-staffing-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web/app/staffing/page.tsx apps/web/e2e/staffing.spec.ts .github/workflows/format-staffing-once.yml
          git commit -m "style: format manual staffing save flow"
          git push origin HEAD:agent/manual-teacher-card-save
