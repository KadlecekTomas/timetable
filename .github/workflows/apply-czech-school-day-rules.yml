name: Disabled Czech school day patcher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot Czech school day patch already completed."
