name: Format rotation Excel once

on:
  pull_request:

permissions:
  contents: write

jobs:
  format:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npx prettier --write apps/web/lib/import/teaching-plan-workbook.ts apps/web/tests/teaching-plan-rotations.test.ts
      - name: Commit formatting
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web/lib/import/teaching-plan-workbook.ts apps/web/tests/teaching-plan-rotations.test.ts
          git commit -m "style(excel): format sport and rotation workbook"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
